#!/usr/bin/env python3
# -*- encoding: UTF-8 -*-

import argparse
import subprocess
import sys
import os
import json

arg_list= list(sys.argv)[1:]

for line in sys.stdin:
  for arg in arg_list:
    print(arg.format(**json.loads(line)))
